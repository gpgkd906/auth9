//! Backward-compatible identity provider service shim.
//!
//! Real implementation lives in `domains::identity::service::identity_provider`.

pub use crate::domains::identity::service::identity_provider::*;
