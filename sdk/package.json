{
  "name": "auth9-sdk",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "express": "^4.22.1",
    "turbo": "^2.3.0"
  }
}
