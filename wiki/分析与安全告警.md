# 分析与安全告警

Auth9 提供全面的登录分析、安全检测和告警功能，帮助管理员监控系统安全状况。

## 核心功能

### 功能概览

| 功能 | 描述 | 用途 |
|------|------|------|
| **登录分析** | 统计登录成功/失败率 | 监控系统使用情况 |
| **事件日志** | 记录所有登录事件 | 审计和调查 |
| **安全检测** | 自动检测异常行为 | 实时安全防护 |
| **安全告警** | 生成和管理告警 | 响应安全威胁 |
| **Webhook** | 实时事件通知 | 集成外部系统 |

## 登录分析

### 查看分析仪表板

**通过管理界面**：
1. 点击左侧导航栏的 **Analytics**
2. 选择时间范围（7/14/30/90天）
3. 查看统计数据和图表

**通过 REST API**：

```bash
curl "https://api.auth9.yourdomain.com/api/v1/analytics/stats?start_date=2024-01-01&end_date=2024-01-31" \
  -H "Authorization: Bearer <admin_token>"
```

响应：

```json
{
  "data": {
    "period": {
      "start_date": "2024-01-01",
      "end_date": "2024-01-31"
    },
    "summary": {
      "total_logins": 15420,
      "successful_logins": 14890,
      "failed_logins": 530,
      "success_rate": 96.56,
      "unique_users": 2340
    },
    "by_event_type": {
      "login_success": 14500,
      "social_login": 390,
      "wrong_password": 420,
      "mfa_failed": 80,
      "account_locked": 30
    },
    "by_device_type": {
      "desktop": 9800,
      "mobile": 4520,
      "tablet": 1100
    },
    "by_day": [
      { "date": "2024-01-01", "total": 498, "success": 485, "failed": 13 },
      { "date": "2024-01-02", "total": 512, "success": 501, "failed": 11 }
    ]
  }
}
```

### 统计指标说明

| 指标 | 说明 | 计算方式 |
|------|------|---------|
| **Total Logins** | 总登录次数 | 所有登录尝试 |
| **Successful Logins** | 成功登录次数 | 认证通过的登录 |
| **Failed Logins** | 失败登录次数 | 认证失败的登录 |
| **Success Rate** | 成功率 | 成功 / 总数 × 100% |
| **Unique Users** | 独立用户数 | 去重的用户数量 |

## 登录事件

### 事件类型

| 事件类型 | 描述 | 严重级别 |
|---------|------|---------|
| `login_success` | 登录成功 | Info |
| `social_login` | 社交登录成功 | Info |
| `wrong_password` | 密码错误 | Warning |
| `mfa_failed` | MFA 验证失败 | Warning |
| `account_locked` | 账户被锁定 | High |
| `invalid_token` | 无效令牌 | Warning |
| `session_expired` | 会话过期 | Info |

### 查询登录事件

```bash
curl "https://api.auth9.yourdomain.com/api/v1/analytics/events?page=1&per_page=50" \
  -H "Authorization: Bearer <admin_token>"
```

响应：

```json
{
  "data": [
    {
      "id": "event-uuid",
      "event_type": "login_success",
      "user_id": "user-uuid",
      "user_email": "user@example.com",
      "ip_address": "192.168.1.100",
      "device_type": "desktop",
      "browser": "Chrome 120",
      "os": "macOS",
      "location": "Tokyo, Japan",
      "details": null,
      "created_at": "2024-01-01T12:00:00Z"
    },
    {
      "id": "event-uuid-2",
      "event_type": "wrong_password",
      "user_id": "user-uuid",
      "user_email": "user@example.com",
      "ip_address": "203.0.113.50",
      "device_type": "mobile",
      "details": {
        "reason": "invalid_credentials"
      },
      "created_at": "2024-01-01T11:55:00Z"
    }
  ],
  "pagination": {
    "total": 15420,
    "page": 1,
    "per_page": 50,
    "total_pages": 309
  }
}
```

### 筛选事件

支持的筛选参数：

| 参数 | 类型 | 说明 |
|------|------|------|
| `event_type` | string | 事件类型 |
| `user_id` | UUID | 用户 ID |
| `start_date` | date | 开始日期 |
| `end_date` | date | 结束日期 |
| `ip_address` | string | IP 地址 |

示例：

```bash
curl "https://api.auth9.yourdomain.com/api/v1/analytics/events?event_type=wrong_password&start_date=2024-01-01" \
  -H "Authorization: Bearer <admin_token>"
```

## 安全检测

### 检测规则

Auth9 内置以下安全检测规则：

#### 1. 暴力破解检测 (Brute Force)

```json
{
  "rule": "brute_force",
  "condition": {
    "failed_attempts": 5,
    "time_window_minutes": 5,
    "same_user": true
  },
  "severity": "high",
  "action": "lock_account"
}
```

#### 2. 密码喷洒检测 (Password Spray)

```json
{
  "rule": "password_spray",
  "condition": {
    "failed_attempts": 10,
    "time_window_minutes": 10,
    "same_ip": true,
    "different_users": true
  },
  "severity": "critical",
  "action": "block_ip"
}
```

#### 3. 不可能的旅行 (Impossible Travel)

```json
{
  "rule": "impossible_travel",
  "condition": {
    "distance_km": 500,
    "time_hours": 1
  },
  "severity": "medium",
  "action": "alert"
}
```

#### 4. 新设备登录

```json
{
  "rule": "new_device",
  "condition": {
    "first_time_device": true
  },
  "severity": "low",
  "action": "notify"
}
```

#### 5. 可疑 IP 检测

```json
{
  "rule": "suspicious_ip",
  "condition": {
    "ip_reputation": "low",
    "tor_exit_node": true,
    "vpn_detected": true
  },
  "severity": "medium",
  "action": "mfa_required"
}
```

### 检测服务架构

```
登录事件 → 事件队列 → 检测引擎 → 规则匹配 → 生成告警
                                       ↓
                              触发响应动作
```

## 安全告警

### 告警属性

| 属性 | 类型 | 说明 |
|------|------|------|
| `id` | UUID | 告警唯一标识 |
| `type` | string | 告警类型 |
| `severity` | enum | 严重级别 |
| `user_id` | UUID | 相关用户 |
| `details` | JSON | 详细信息 |
| `resolved` | boolean | 是否已解决 |
| `resolved_at` | timestamp | 解决时间 |
| `created_at` | timestamp | 创建时间 |

### 告警类型

| 类型 | 描述 | 默认严重级别 |
|------|------|-------------|
| `brute_force` | 暴力破解尝试 | Critical |
| `password_spray` | 密码喷洒攻击 | Critical |
| `impossible_travel` | 不可能的旅行 | High |
| `new_device` | 新设备登录 | Low |
| `suspicious_ip` | 可疑 IP | Medium |
| `account_locked` | 账户被锁定 | High |

### 严重级别

| 级别 | 描述 | 响应时间建议 |
|------|------|-------------|
| 🔴 **Critical** | 严重威胁 | 立即处理 |
| 🟠 **High** | 高危威胁 | 1小时内 |
| 🟡 **Medium** | 中等风险 | 24小时内 |
| 🟢 **Low** | 低风险 | 定期审查 |

### 查看告警

**通过管理界面**：
1. 导航到 **Security** > **Alerts**
2. 使用筛选器查看不同状态的告警
3. 点击告警查看详情

**通过 REST API**：

```bash
curl "https://api.auth9.yourdomain.com/api/v1/security/alerts?page=1&per_page=20&unresolved_only=true" \
  -H "Authorization: Bearer <admin_token>"
```

响应：

```json
{
  "data": [
    {
      "id": "alert-uuid",
      "type": "brute_force",
      "severity": "critical",
      "user_id": "user-uuid",
      "user_email": "user@example.com",
      "details": {
        "failed_attempts": 10,
        "time_window_minutes": 5,
        "ip_address": "203.0.113.50",
        "last_attempt_at": "2024-01-01T12:05:00Z"
      },
      "resolved": false,
      "created_at": "2024-01-01T12:05:00Z"
    }
  ],
  "pagination": {
    "total": 5,
    "page": 1,
    "per_page": 20,
    "unresolved_count": 5
  }
}
```

### 解决告警

```bash
curl -X POST https://api.auth9.yourdomain.com/api/v1/security/alerts/{alert_id}/resolve \
  -H "Authorization: Bearer <admin_token>" \
  -H "Content-Type: application/json" \
  -d '{
    "resolution_note": "已验证为正常行为，用户确认"
  }'
```

响应：

```json
{
  "data": {
    "id": "alert-uuid",
    "resolved": true,
    "resolved_at": "2024-01-01T14:00:00Z",
    "resolved_by": "admin-uuid",
    "resolution_note": "已验证为正常行为，用户确认"
  },
  "message": "Alert resolved successfully."
}
```

## Webhook 通知

### 支持的事件

| 事件 | 描述 | 触发时机 |
|------|------|---------|
| `login.success` | 登录成功 | 用户登录成功 |
| `login.failed` | 登录失败 | 用户登录失败 |
| `user.created` | 用户创建 | 新用户注册 |
| `user.updated` | 用户更新 | 用户信息变更 |
| `user.deleted` | 用户删除 | 用户被删除 |
| `password.changed` | 密码修改 | 用户修改密码 |
| `mfa.enabled` | MFA 启用 | 用户启用 MFA |
| `mfa.disabled` | MFA 禁用 | 用户禁用 MFA |
| `session.revoked` | 会话撤销 | 会话被撤销 |
| `security.alert` | 安全告警 | 生成新告警 |

### 创建 Webhook

**通过管理界面**：
1. 导航到 **Tenants** > 选择租户 > **Webhooks**
2. 点击 **Add Webhook**
3. 填写配置信息
4. 选择订阅的事件
5. 点击 **Create**

**通过 REST API**：

```bash
curl -X POST https://api.auth9.yourdomain.com/api/v1/tenants/{tenant_id}/webhooks \
  -H "Authorization: Bearer <admin_token>" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Security Alerts",
    "url": "https://hooks.example.com/auth9",
    "secret": "your-webhook-secret",
    "events": ["security.alert", "login.failed", "session.revoked"],
    "enabled": true
  }'
```

### Webhook 请求格式

```http
POST /your-webhook-endpoint HTTP/1.1
Host: hooks.example.com
Content-Type: application/json
X-Auth9-Signature: sha256=abc123...
X-Auth9-Event: security.alert
X-Auth9-Delivery-Id: delivery-uuid

{
  "event": "security.alert",
  "timestamp": "2024-01-01T12:05:00Z",
  "data": {
    "alert_id": "alert-uuid",
    "type": "brute_force",
    "severity": "critical",
    "user_id": "user-uuid",
    "details": {...}
  }
}
```

### 签名验证

```javascript
const crypto = require('crypto');

function verifyWebhookSignature(payload, signature, secret) {
  const expectedSignature = 'sha256=' + crypto
    .createHmac('sha256', secret)
    .update(payload)
    .digest('hex');

  return crypto.timingSafeEqual(
    Buffer.from(signature),
    Buffer.from(expectedSignature)
  );
}

// Express 中间件示例
app.post('/webhook', express.raw({ type: 'application/json' }), (req, res) => {
  const signature = req.headers['x-auth9-signature'];
  const payload = req.body;

  if (!verifyWebhookSignature(payload, signature, process.env.WEBHOOK_SECRET)) {
    return res.status(401).send('Invalid signature');
  }

  const event = JSON.parse(payload);
  // 处理事件...

  res.status(200).send('OK');
});
```

### 测试 Webhook

```bash
curl -X POST https://api.auth9.yourdomain.com/api/v1/tenants/{tenant_id}/webhooks/{webhook_id}/test \
  -H "Authorization: Bearer <admin_token>"
```

响应：

```json
{
  "data": {
    "success": true,
    "status_code": 200,
    "response_time_ms": 125
  }
}
```

### 重试机制

Webhook 投递失败时的重试策略：

| 重试次数 | 延迟时间 |
|---------|---------|
| 1 | 1 分钟 |
| 2 | 5 分钟 |
| 3 | 30 分钟 |
| 4 | 2 小时 |
| 5 | 24 小时 |

超过 5 次重试后，Webhook 会被标记为失败。

## 数据库结构

### 登录事件表

```sql
CREATE TABLE login_events (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id UUID REFERENCES tenants(id),
  user_id UUID REFERENCES users(id),
  event_type VARCHAR(50) NOT NULL,
  ip_address VARCHAR(45),
  device_type VARCHAR(50),
  browser VARCHAR(255),
  os VARCHAR(255),
  location VARCHAR(255),
  user_agent TEXT,
  details JSONB,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,

  INDEX idx_tenant_id (tenant_id),
  INDEX idx_user_id (user_id),
  INDEX idx_event_type (event_type),
  INDEX idx_created_at (created_at),
  INDEX idx_ip_address (ip_address)
);
```

### 安全告警表

```sql
CREATE TABLE security_alerts (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id UUID REFERENCES tenants(id),
  user_id UUID REFERENCES users(id),
  type VARCHAR(50) NOT NULL,
  severity VARCHAR(20) NOT NULL,
  details JSONB NOT NULL,
  resolved BOOLEAN NOT NULL DEFAULT FALSE,
  resolved_at TIMESTAMP,
  resolved_by UUID REFERENCES users(id),
  resolution_note TEXT,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,

  INDEX idx_tenant_id (tenant_id),
  INDEX idx_user_id (user_id),
  INDEX idx_type (type),
  INDEX idx_severity (severity),
  INDEX idx_resolved (resolved),
  INDEX idx_created_at (created_at)
);
```

### Webhook 表

```sql
CREATE TABLE webhooks (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id UUID NOT NULL REFERENCES tenants(id),
  name VARCHAR(255) NOT NULL,
  url VARCHAR(2048) NOT NULL,
  secret VARCHAR(255),
  events TEXT[] NOT NULL,
  enabled BOOLEAN NOT NULL DEFAULT TRUE,
  failure_count INTEGER NOT NULL DEFAULT 0,
  last_triggered_at TIMESTAMP,
  last_success_at TIMESTAMP,
  last_failure_at TIMESTAMP,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,

  INDEX idx_tenant_id (tenant_id),
  INDEX idx_enabled (enabled)
);
```

## 最佳实践

### 1. 监控建议

- 每日审查 Critical 和 High 告警
- 设置 Webhook 通知到 Slack/Teams
- 监控登录失败率趋势
- 定期审查审计日志

### 2. 告警响应

| 告警类型 | 建议响应 |
|---------|---------|
| 暴力破解 | 验证用户、考虑锁定账户 |
| 密码喷洒 | 阻止 IP、通知相关用户 |
| 不可能的旅行 | 联系用户确认、可能撤销会话 |
| 新设备 | 通知用户、考虑额外验证 |

### 3. Webhook 配置

- 使用 HTTPS 端点
- 配置签名验证
- 实现幂等处理
- 处理重试场景

### 4. 数据保留

- 登录事件建议保留 90 天
- 安全告警建议保留 1 年
- 定期清理过期数据

## 故障排查

### 告警未生成

检查项：
1. 检测规则是否启用
2. 事件是否正确记录
3. 检测服务是否运行

### Webhook 未触发

检查项：
1. Webhook 是否启用
2. 订阅的事件是否正确
3. 端点 URL 是否可访问
4. 查看失败次数和错误信息

### 统计数据不准确

检查项：
1. 时间范围选择是否正确
2. 事件是否正确记录
3. 是否存在时区问题

## 常见问题

### Q: 如何自定义检测规则？

A: 当前版本使用内置规则，自定义规则将在后续版本支持。

### Q: 告警太多怎么办？

A: 可以调整检测规则的阈值，或者配置告警聚合减少噪音。

### Q: 如何导出登录事件？

A: 使用 API 分页获取所有事件，或者直接从数据库导出。

### Q: Webhook 失败会丢失事件吗？

A: 不会。系统会重试最多 5 次，失败的事件会记录在日志中。

## 相关文档

- [会话管理](会话管理.md)
- [密码管理](密码管理.md)
- [REST API](REST-API.md)
- [最佳实践](最佳实践.md)
