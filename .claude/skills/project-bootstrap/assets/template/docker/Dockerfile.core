FROM rust:1.80 as builder
WORKDIR /app
COPY . .
RUN cargo build --release

FROM debian:bookworm-slim
WORKDIR /app
COPY --from=builder /app/target/release/{{project_name}}-core /app/{{project_name}}-core
EXPOSE {{core_port}}
CMD ["/app/{{project_name}}-core"]
