apiVersion: v1
kind: Service
metadata:
  name: {{project_name}}-core
  namespace: {{namespace}}
spec:
  selector:
    app: {{project_name}}-core
  ports:
    - name: http
      port: {{core_port}}
      targetPort: {{core_port}}
