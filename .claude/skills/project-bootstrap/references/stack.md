# Stack Options

Default stack:
- Rust (axum + tokio)
- React Router 7 + TypeScript + Vite
- Docker (compose + Dockerfiles)
- Kubernetes (base manifests)

Optional extensions:
- Add Keycloak, DB, Redis, Mailpit by extending docker-compose and k8s manifests.
