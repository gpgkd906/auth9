<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Auth9 Demo - SDK Integration Guide</title>
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: #0f1117;
      color: #e1e4e8;
      line-height: 1.6;
      padding: 2rem;
      max-width: 960px;
      margin: 0 auto;
    }

    h1 {
      color: #58a6ff;
      margin-bottom: 0.5rem;
      font-size: 2rem;
    }

    h2 {
      color: #79c0ff;
      margin: 2rem 0 1rem;
      font-size: 1.3rem;
      border-bottom: 1px solid #21262d;
      padding-bottom: 0.5rem;
    }

    h3 {
      color: #d2a8ff;
      margin: 1.5rem 0 0.5rem;
      font-size: 1.1rem;
    }

    p {
      margin: 0.5rem 0;
      color: #8b949e;
    }

    .subtitle {
      color: #8b949e;
      font-size: 1rem;
      margin-bottom: 2rem;
    }

    .card {
      background: #161b22;
      border: 1px solid #21262d;
      border-radius: 8px;
      padding: 1.2rem;
      margin: 0.8rem 0;
    }

    .method {
      display: inline-block;
      padding: 2px 8px;
      border-radius: 4px;
      font-size: 0.75rem;
      font-weight: 700;
      margin-right: 8px;
      font-family: monospace;
    }

    .get {
      background: #1f6feb33;
      color: #58a6ff;
    }

    .post {
      background: #238636;
      color: #3fb950;
    }

    .endpoint {
      font-family: monospace;
      color: #f0883e;
      font-weight: 600;
    }

    .hint {
      color: #8b949e;
      font-size: 0.9rem;
      margin-top: 4px;
    }

    .tag {
      display: inline-block;
      padding: 2px 8px;
      border-radius: 12px;
      font-size: 0.7rem;
      font-weight: 600;
      margin-left: 8px;
    }

    .public {
      background: #238636;
      color: #3fb950;
    }

    .protected {
      background: #da3633;
      color: #f85149;
    }

    .rbac {
      background: #8957e5;
      color: #d2a8ff;
    }

    .grpc-tag {
      background: #1f6feb;
      color: #58a6ff;
    }

    .mgmt {
      background: #f0883e33;
      color: #f0883e;
    }

    code {
      background: #21262d;
      padding: 2px 6px;
      border-radius: 4px;
      font-size: 0.85rem;
      color: #79c0ff;
    }

    pre {
      background: #161b22;
      border: 1px solid #21262d;
      border-radius: 8px;
      padding: 1rem;
      overflow-x: auto;
      font-size: 0.85rem;
      margin: 0.5rem 0;
      color: #c9d1d9;
    }

    .config-table {
      width: 100%;
      border-collapse: collapse;
      margin: 0.5rem 0;
    }

    .config-table td {
      padding: 6px 8px;
      border-bottom: 1px solid #21262d;
    }

    .config-table td:first-child {
      color: #79c0ff;
      font-family: monospace;
      white-space: nowrap;
    }

    .config-table td:last-child {
      color: #8b949e;
    }

    .flow {
      background: #0d1117;
      border: 1px solid #30363d;
      border-radius: 8px;
      padding: 1rem;
      margin: 1rem 0;
      font-family: monospace;
      font-size: 0.85rem;
      color: #8b949e;
    }

    .flow .arrow {
      color: #58a6ff;
    }

    a {
      color: #58a6ff;
      text-decoration: none;
    }

    a:hover {
      text-decoration: underline;
    }

    .form-inline {
      display: flex;
      gap: 8px;
      flex-wrap: wrap;
      align-items: center;
      justify-content: center;
      margin-top: 10px;
    }

    .input {
      background: #0d1117;
      color: #e1e4e8;
      border: 1px solid #30363d;
      border-radius: 6px;
      padding: 8px 10px;
      min-width: 260px;
      font-size: 0.95rem;
    }

    .btn {
      display: inline-block;
      border: 1px solid #30363d;
      border-radius: 6px;
      padding: 8px 16px;
      font-weight: 600;
      text-decoration: none;
      cursor: pointer;
      background: #1f6feb;
      color: #fff;
    }

    .btn.secondary {
      background: #21262d;
      color: #f0f6fc;
    }
  </style>
</head>

<body>
  <h1>🔐 Auth9 Demo</h1>
  <p class="subtitle">SDK integration examples for authentication, RBAC, and multi-tenant management</p>

  <div class="card" style="text-align: center; background: #0d1117; border-color: #30363d;">
    <% if (user) { %>
      <p style="margin-bottom: 1rem; color: #58a6ff;">Welcome, <strong>
          <%= user.name || user.email %>
        </strong>!</p>
      <a href="/dashboard"
        style="display: inline-block; background: #1f6feb; color: white; padding: 8px 16px; border-radius: 6px; text-decoration: none; font-weight: 600; margin-right: 10px;">
        Go to Dashboard
      </a>
      <a href="/logout"
        style="display: inline-block; background: #21262d; color: #f85149; border: 1px solid #30363d; padding: 8px 16px; border-radius: 6px; text-decoration: none; font-weight: 600;">
        Logout
      </a>
      <% } else { %>
        <p style="margin-bottom: 1rem;">You are currently not logged in.</p>
        <a href="/login"
          style="display: inline-block; background: #238636; color: white; padding: 8px 20px; border-radius: 6px; text-decoration: none; font-weight: 600; font-size: 1.1rem;">
          Login with Auth9
        </a>
        <form method="POST" action="/enterprise/login" class="form-inline">
          <input class="input" type="email" name="email" placeholder="qa-user@corp.example.com" required>
          <button class="btn secondary" type="submit">Login with Enterprise SSO</button>
        </form>
        <p class="hint">企业邮箱登录将先调用域名发现，再重定向到企业 IdP。</p>
        <% } %>
  </div>

  <h2>Available Endpoints</h2>

  <div class="card">
    <span class="method get">GET</span>
    <span class="endpoint">/health</span>
    <span class="tag public">Public</span>
    <p class="hint">Health check - no authentication required</p>
  </div>

  <h3>Token Verification (auth9Middleware)</h3>

  <div class="card">
    <span class="method get">GET</span>
    <span class="endpoint">/api/me</span>
    <span class="tag protected">Token Required</span>
    <p class="hint">Returns current user info extracted from the verified JWT token</p>
  </div>

  <h3>RBAC Guards (requireRole / requirePermission)</h3>

  <div class="card">
    <span class="method get">GET</span>
    <span class="endpoint">/api/admin</span>
    <span class="tag rbac">Role: admin</span>
    <p class="hint">Requires <code>admin</code> role in the Tenant Access Token</p>
  </div>

  <div class="card">
    <span class="method get">GET</span>
    <span class="endpoint">/api/resources</span>
    <span class="tag rbac">Perm: resource:read</span>
    <p class="hint">Requires <code>resource:read</code> permission in the Tenant Access Token</p>
  </div>

  <h3>gRPC Integration (Auth9GrpcClient)</h3>

  <div class="card">
    <span class="method post">POST</span>
    <span class="endpoint">/demo/exchange-token</span>
    <span class="tag grpc-tag">gRPC</span>
    <span class="tag protected">Token Required</span>
    <p class="hint">Exchange Identity Token → Tenant Access Token with roles/permissions</p>
    <pre>curl -X POST http://localhost:3002/demo/exchange-token \
  -H "Authorization: Bearer &lt;identity-token&gt;" \
  -H "Content-Type: application/json" \
  -d '{"tenantId": "&lt;tenant-id&gt;", "serviceId": "&lt;service-id&gt;"}'</pre>
  </div>

  <div class="card">
    <span class="method post">POST</span>
    <span class="endpoint">/demo/introspect</span>
    <span class="tag grpc-tag">gRPC</span>
    <span class="tag protected">Token Required</span>
    <p class="hint">Introspect a token to see its full claims (sub, email, roles, permissions, etc.)</p>
    <pre>curl -X POST http://localhost:3002/demo/introspect \
  -H "Authorization: Bearer &lt;token&gt;"</pre>
  </div>

  <h3>Management API (Auth9HttpClient)</h3>

  <div class="card">
    <span class="method get">GET</span>
    <span class="endpoint">/demo/tenants</span>
    <span class="tag mgmt">Management</span>
    <span class="tag protected">Token Required</span>
    <p class="hint">List all tenants via Auth9 Core REST API</p>
  </div>

  <div class="card">
    <span class="method get">GET</span>
    <span class="endpoint">/demo/users</span>
    <span class="tag mgmt">Management</span>
    <span class="tag protected">Token Required</span>
    <p class="hint">List all users via Auth9 Core REST API</p>
  </div>

  <h3>Enterprise SSO Test Endpoints</h3>

  <div class="card">
    <span class="method post">POST</span>
    <span class="endpoint">/enterprise/login</span>
    <span class="tag public">Public</span>
    <p class="hint">输入企业邮箱触发 discovery 并跳转到企业 IdP。</p>
  </div>

  <div class="card">
    <span class="method post">POST</span>
    <span class="endpoint">/demo/enterprise/discovery</span>
    <span class="tag public">Public</span>
    <p class="hint">用于 QA 脚本验证域名路由，返回 Core discovery 响应。</p>
  </div>

  <div class="card">
    <span class="method get">GET</span>
    <span class="endpoint">/demo/enterprise/connectors?tenantId=&lt;tenant-id&gt;</span>
    <span class="tag mgmt">Management</span>
    <p class="hint">查询租户企业连接器（需配置 <code>AUTH9_ADMIN_TOKEN</code>）。</p>
  </div>

  <div class="card">
    <span class="method post">POST</span>
    <span class="endpoint">/demo/enterprise/connectors</span>
    <span class="tag mgmt">Management</span>
    <p class="hint">创建连接器；更新、删除、测试见 Dashboard 的 QA 面板。</p>
  </div>

  <h2>Auth Flow</h2>
  <div class="flow">
    User Login (Keycloak) <span class="arrow">→</span> Identity Token<br>
    <span class="arrow">↓</span><br>
    Token Exchange (gRPC) <span class="arrow">→</span> Tenant Access Token (with roles &amp; permissions)<br>
    <span class="arrow">↓</span><br>
    Access Protected API <span class="arrow">→</span> auth9Middleware verifies token<br>
    <span class="arrow">↓</span><br>
    RBAC Check <span class="arrow">→</span> requireRole / requirePermission guards
  </div>

  <h2>Quick Start</h2>
  <pre># Using curl with a token obtained from Auth9
TOKEN="your-auth9-token"

# Check your identity
curl -H "Authorization: Bearer $TOKEN" http://localhost:3002/api/me

# Exchange for a tenant-scoped token
curl -X POST http://localhost:3002/demo/exchange-token \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"tenantId": "your-tenant-id", "serviceId": "your-service-id"}'

# Use the tenant access token for RBAC-protected endpoints
curl -H "Authorization: Bearer $TENANT_TOKEN" http://localhost:3002/api/admin
curl -H "Authorization: Bearer $TENANT_TOKEN" http://localhost:3002/api/resources</pre>

  <h2>Configuration</h2>
  <table class="config-table">
    <tr>
      <td>AUTH9_DOMAIN</td>
      <td>
        <%= config.auth9Domain %>
      </td>
    </tr>
    <tr>
      <td>AUTH9_GRPC_API_KEY</td>
      <td>
        <%= config.auth9GrpcApiKey %>
      </td>
    </tr>
    <tr>
      <td>AUTH9_PUBLIC_URL</td>
      <td>
        <%= config.auth9PublicUrl %>
      </td>
    </tr>
    <tr>
      <td>AUTH9_GRPC_ADDRESS</td>
      <td>
        <%= config.auth9GrpcAddress %>
      </td>
    </tr>
    <tr>
      <td>AUTH9_AUDIENCE</td>
      <td>
        <%= config.auth9Audience %>
      </td>
    </tr>
    <tr>
      <td>PORT</td>
      <td>
        <%= config.port %>
      </td>
    </tr>
    <tr>
      <td>AUTH9_DEFAULT_TENANT_ID</td>
      <td>
        <%= config.defaultTenantId %>
      </td>
    </tr>
  </table>

  <h2>SDK Packages Used</h2>
  <div class="card">
    <p><code>@auth9/node</code> — Main SDK: Auth9 class, TokenVerifier, Auth9GrpcClient</p>
    <p><code>@auth9/node/middleware/express</code> — Express middleware: auth9Middleware, requireRole, requirePermission
    </p>
    <p><code>@auth9/core</code> — HTTP client &amp; domain types: Auth9HttpClient, Tenant, User, Role, etc.</p>
  </div>
</body>

</html>
